{
  "name": "cybershield-x-root",
  "version": "1.0.0",
  "scripts": {
    "frontend": "npm run dev --prefix frontend",
    "backend": "cd backend && uvicorn app.main:app --reload",
    "dev": "npm run frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run dev"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
